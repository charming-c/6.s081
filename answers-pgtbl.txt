1. 初始化进程的前三页内容：
    page 0：是 init 程序的代码和数据
    page 1：guard page， 用于防止栈溢出，修改程序本身
    page 2：是用户空间栈

    由于 page1 的 PTE_U 设置为 0，所以用户是不能够访问的

2. 为什么有  srcva + len < srcva 判断？
    这是因为程序的内核页表将 PTE_U 都设置为 0， 所以在 S 模式下都是可以访问的，
    如果 len 可以为负数，这又可以修改程序本身的代码和数据。